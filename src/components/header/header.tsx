import styles from './header.module.scss';

export default function Header() {
  return (
    <header className={styles.header}>
      {/* <img className={styles.logo} src="/img/logo_urfu.svg" alt=""/> */}
    </header>
  )
}
